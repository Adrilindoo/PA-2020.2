/*#include <iostream>
#include <vector>
using namespace std;

int main(){
    vector<int> v; //um vetor de inteiros vazio
    v.push_back(4); //o último elemento de v é 4
    v.push_back(5); //o último elemento de v é 5
    v.push_back(6); //o último elemento de v é 6

    //MOSTRANDO OS ELEMENTOS DE V
        for (int i=0; i < v.size(); i++){
            cout << v[i];
            if (i != v.size() - 1){
                cout << ", ";
            }
        }
        cout << endl;

    v.push_back(7); //o último elemento de v é 7
        for (int i=0; i < v.size(); i++){
            cout << v[i];
            if (i != v.size() - 1){
                cout << ", ";
            }
        }
        cout << endl;

    v.pop_back(); //removendo o último elemento de v
        for (int i=0; i < v.size(); i++){
            cout << v[i];
            if (i != v.size() - 1){
                cout << ", ";
            }
        }
        cout << endl;

    v.clear(); //limpando todos os elementos de v
        for (int i=0; i < v.size(); i++){
        cout << v[i];
        if (i != v.size() - 1){
            cout << ", ";
        }
    }
        cout << endl;

}*/