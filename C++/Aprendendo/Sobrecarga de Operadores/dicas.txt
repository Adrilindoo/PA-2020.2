NÃO USAR O MAKEFILE
pode fazaer pelo terminal usando o seguinte comando:
    g++ main.cpp -o main
    ./main
(a palavra main pode ser substituída, desde que o arquivo que contém a função main possua outro nome)